{
  "name": "@local/mcp-prompt-optimizer",
  "version": "1.0.0",
  "description": "MCP server that optimizes prompts using the OTA Framework",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "mcp-prompt-optimizer": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "prepare": "npm run build"
  },
  "keywords": ["mcp", "prompt-optimization", "ai"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  }
}
